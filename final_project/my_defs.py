#!usr/bin/python

def getFrac(nom, denom):
    frac = 0.
    if denom == 'NaN':
        return frac
    elif denom == 0
        return frac
    else:
        if nom == 'NaN'
            return frac
        else:
            frac = float(nom)/denom
    return frac

#end of file
